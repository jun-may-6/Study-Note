# spring datasource config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/menudb
    username: ohgiraffers
    password: ohgiraffers

# mybatis config
mybatis:
  mapper-locations: mappers/**/*.xml
  configuration:
    map-underscore-to-camel-case: true    #구분자 선언

# loging config
logging:
#  level:
#    root: debug
  file:
    name: myapp.log
    path: C:/dev/lecture-code/chap08-crud
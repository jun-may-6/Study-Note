/* 04. undifiend & null */

/* undifiend 타입
    var 키워드로 선언한 변수는 암묵적으로 undefined 로 초기화 되므로
    변수로 선언한 이후 값을 할당하지 않은 변수를 참조하면 undefined 로 반환된다.
 */

var undef;
console.log(undef);

/* null 타입 */
/* 변수에 값이 없다는 것을 의도적으로 명시할 때 사용한다. */

var nul = null;
console.log(nul);